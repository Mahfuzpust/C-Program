#include<iostream>
using namespace std;
int fact(int n)
{
    int i,f=1;
    for(i=1;i<=n;i++)
    {
        f=f*i;
    }
    return f;
}
int main()
{
    int n,r;
    cin >> n >> r;
    int nPr=fact(n)/fact(n-r);
    cout << "The nPr : " << nPr;
}
