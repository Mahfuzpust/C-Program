#include<stdio.h>
int cheakprime(int n)
{
    int i,flag=0;
    for(i=2;i<n;i++)
    {
        if(n%i==0)
            flag++;
            break;
    }
    if(flag==1)
    {
        printf("Not Prime");
    }
    else{
        printf("Prime");
    }
}

int main()
{
    int n,m;
    scanf("%d",&n);
    m=cheakprime(n);
    return m;
}
